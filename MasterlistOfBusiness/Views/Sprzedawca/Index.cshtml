@model IEnumerable<MasterlistOfBusiness.Models.Sprzedawca>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.login)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.login)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_sprzedawcy">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_sprzedawcy">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_sprzedawcy">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
